name: jejakdonaturapp
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.4
  cupertino_icons: ^1.0.8
  share_plus: ^7.2.1
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/sakukasihlogo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/icon/sakukasihlogo.png
    - assets/images/